apiVersion: v1
kind: PersistentVolume
metadata:
  name: plf-dev-postgresql-pv
spec:
  persistentVolumeReclaimPolicy: Recycle
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/var/lib/postgresql/data"
