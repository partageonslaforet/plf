apiVersion: v1
kind: Pod
metadata:
  name: plf-dev-env
spec:
  resources:
  containers:
  - name: plf-dev-env
    image: php:latest
    volumeMounts:
    - name: config-volume
      mountPath: /etc/config
volumes:
- name: config-volume
  configMap:
    name: my-configmap